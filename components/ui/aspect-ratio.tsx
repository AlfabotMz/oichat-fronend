"use client"


import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio" // This line is already correct, no change needed.

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
